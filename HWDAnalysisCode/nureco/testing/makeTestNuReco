CC=g++ -Wall -Wextra -O3 -g
CFLAGS= -Wl,--no-as-needed
LDFLAGS=`root-config --glibs --cflags`
SOURCES= testNuReco.cc ../nuRecoLibrary.o
OBJECTS=$(SOURCES:.cc=.o)
EXECUTABLE=testNuReco

all: 
	$(CC) $(CFLAGS) $(SOURCES) $(LDFLAGS) -o $(EXECUTABLE)
	
clean:
	rm -rf *o $(EXECUTABLE)
